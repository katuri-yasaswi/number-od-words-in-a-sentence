#include <stdio.h>
int fact(int);
int fact(int a)
{
    if(a==1)
    {
         return 1;
    }
    else
    {
        return a*fact(a-1);
    }
}
void main()
{
    int a;
    printf("enter a value:");
    scanf("%d",&a);
    printf("the factorial of number is :%d",fact(a));
}


Output:

enter a value:5
the factorial of number is :120
